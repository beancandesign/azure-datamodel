name: TestPR

on:
  pull_request:
    branches:
        - 'main'
    types: [opened, ready_for_review]

jobs:
  test_pr:
    runs-on: ubuntu-latest
    steps:
      - name: Run test workflow
        uses: beancandesign/oswdatamodel/.github/workflows/run_tests.yml